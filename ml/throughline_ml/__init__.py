"""Throughline ML — deterministic parsers + CP-SAT scheduler + (scaffolded) breakdown/RAG.

Doctrine (PRODUCT_SPEC §12): right tool per task — deterministic solvers for math,
open LLMs for language, deterministic parsers for file-format fidelity. AI never owns
format fidelity (FDX/AICP/MM); AI output is always a draft with confidence + provenance.
"""

__version__ = "0.1.0"
