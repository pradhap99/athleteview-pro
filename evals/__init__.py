"""Throughline evals — the quality firewall (PRODUCT_SPEC §15).

Capability evals start low (a hill to climb); regression evals sit near 100% and block
releases on any drop; solved capability tasks graduate into the regression suite; every
production failure becomes a golden case.
"""
